#include <stdlib.h>
#include <strings.h>

/*
 * Similar to Python's and Go's split function,
 * Split will return take in an array of strings and a delimiter
 * */
void SplitDelimeter(const char *string, const char delimiter, char *array[]) 
{
	size_t len =  strlen(string);
}
